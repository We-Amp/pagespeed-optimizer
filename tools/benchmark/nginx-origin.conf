server {
    listen 8081;
    root /usr/share/nginx/html;

    # Cache-Control required for PageSpeed to cache responses.
    # Without it, PageSpeed defaults to max-age=0 (uncacheable).
    add_header Cache-Control "public, max-age=3600";

    location / {
        try_files $uri $uri/ =404;
    }
}
