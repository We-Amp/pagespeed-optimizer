# Compose override: use pre-built CI artifacts instead of building from source.
#
# Usage:
#   docker compose -f tools/stress/docker-compose.yml \
#                  -f tools/stress/docker-compose.prebuilt.yml up -d

services:
  worker:
    build:
      context: ../../
      dockerfile: tools/e2e/Dockerfile.prebuilt
  nginx:
    build:
      context: ../../
      dockerfile: tools/e2e/Dockerfile.prebuilt
